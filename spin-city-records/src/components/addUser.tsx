import { api } from '../utils/api'


export default function AddUser() {

  const handleClick = () => {
    api
  }
  return (
    <> 
      <button onClick={() => handleClick()}>
        Add User
      </button>
    </>
  )

}